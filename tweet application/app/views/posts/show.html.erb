<%= link_to "Back", posts_path%>
<h3><%= @post.creator%></h3>
<p><%= @post.content%></p>

<%= link_to "Edit Post", edit_post_path(@post)%>
<%= button_to "Delete Post", @post, method: :delete, data: {
    turbo_confirm: "Are you sure?"
}%>
